const srcDir = 'src'

const dir = {
  assets: 'assets',
  layouts: 'template/layouts',
  middleware: 'middleware',
  pages: 'template/pages',
  static: 'public',
  store: 'store',
}

export default { srcDir, dir }
