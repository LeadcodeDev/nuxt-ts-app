const srcDir = 'src'

const dir = {
  assets: 'assets',
  layouts: 'template/layouts',
  middleware: 'middlewares',
  pages: 'template/pages',
  static: 'public',
  store: 'store',
}

export default { srcDir, dir }
